#include <stdio.h>
#include <stdlib.h>

int main()
{
    float note,compteur;

     for (note=0;note<=50;note++)
        {
          printf("\n saisir une note:",note);
          scanf("%f",&note);
        }

    return 0;
}
