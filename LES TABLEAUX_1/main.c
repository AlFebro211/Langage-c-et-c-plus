#include <stdio.h>
#include <stdlib.h>

int main()
{
    int tab[4];
    int i=0;
    printf("taper la taille d'un tableau:");
    scanf("%d",&tab[i]);
    for (i=0;i<4;i++)
   {
        printf("%d\n",tab[i]);
   }
    return 0;
}
